// PostCSS configuration for CSS processing pipeline
export default {
  plugins: {
    // TailwindCSS plugin - processes Tailwind utility classes and directives
    tailwindcss: {},
    // Autoprefixer plugin - automatically adds vendor prefixes for cross-browser compatibility
    autoprefixer: {},
  },
}